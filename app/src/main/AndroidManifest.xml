<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_main"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_main_round"
        android:supportsRtl="true"

        android:theme="@style/Theme.UMC_Closit">
        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Login.RegisterActivity" />
        <activity android:name=".Login.MakeIdActivity" />
        <activity android:name=".Community.NewBattleActivity" />
        <activity android:name=".Login.FindID2Activity" />
        <activity android:name=".Login.FindIDActivity" />
        <activity android:name=".Login.FindPassword2Activity" />
        <activity android:name=".Login.FindPasswordActivity" />
        <activity android:name=".Login.PasswordActivity" />
        <activity android:name=".ui.timeline.DetailActivity" />
        <activity android:name=".ui.profile.highlight.AddHighlightActivity" />
        <activity android:name=".ui.timeline.notification.NotificationActivity"/>
        <activity android:name=".ui.profile.history.HistoryActivity" />

        <activity
            android:name=".ui.timeline.TimelineActivity"
            android:exported="true"
            android:theme="@style/Theme.UMC_Closit">
        </activity>
    </application>

</manifest>