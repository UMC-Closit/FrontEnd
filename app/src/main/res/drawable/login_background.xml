<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 배경색 설정 -->
    <solid android:color="@color/check_gray_bright" />

    <!-- 테두리 설정 -->
    <stroke
        android:width="1dp"
        android:color="@color/gray_normal" />

    <!-- 모서리 둥글게 설정 -->
    <corners android:radius="16dp" />

    <!-- 패딩 (선택) -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp" />
</shape>
