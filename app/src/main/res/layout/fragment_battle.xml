<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.community.challenge.ChallengeFragment">

    <!-- 상단 바 (검은 배경) -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/blackbackground"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:background="@drawable/bg_leftcorner_black"
        android:padding="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:id="@+id/title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="배틀게시판"
            android:gravity="center"
            android:textColor="@color/white"
            android:textSize="22sp"
            android:layout_marginTop="16dp"
            android:layout_marginBottom="8dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"/>

        <TextView
            android:id="@+id/title2"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Challenge"
            android:gravity="center"
            android:textColor="@color/white"
            android:textSize="16sp"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="16dp"
            app:layout_constraintTop_toBottomOf="@id/title"
            app:layout_constraintBottom_toTopOf="@id/battleChallengeContainer"
            app:layout_constraintStart_toStartOf="parent"/>

        <!-- item_battle_challenge 추가 (배틀게시판 아래에 작은 크기로 배치) -->
        <LinearLayout
            android:id="@+id/battleChallengeContainer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            app:layout_constraintTop_toBottomOf="@id/title2"
            app:layout_constraintBottom_toBottomOf="parent">

            <!-- 포함된 레이아웃을 감싸는 새로운 LinearLayout 추가 -->
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_horizontal"
                app:layout_constraintTop_toBottomOf="@id/title2"
                >

                <!-- 왼쪽 네모 -->
                <include
                    android:id="@+id/left_item"
                    layout="@layout/item_small_challenge"
                    android:layout_width="45dp"
                    android:layout_height="75dp"/>

                <!-- 오른쪽 네모 -->
                <include
                    android:id="@+id/right_item"
                    layout="@layout/item_small_challenge"
                    android:layout_width="45dp"
                    android:layout_height="75dp"
                    android:layout_marginStart="8dp"/>
            </LinearLayout>
        </LinearLayout>

        <!-- 새로운 배틀 생성 버튼 (오른쪽 아래 배치) -->
        <Button
            android:id="@+id/createButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@android:color/transparent"
            android:textColor="@color/white"
            android:drawableEnd="@drawable/ic_arrow"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:contentDescription="배틀생성"/>
    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- RecyclerView 추가 -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/Battle_recyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/blackbackground"
        app:layout_constraintBottom_toBottomOf="parent"
        android:padding="16dp"
        android:clipToPadding="false"/>

</androidx.constraintlayout.widget.ConstraintLayout>
